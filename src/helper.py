"""Helper includes various mini functions."""


def print_header(text: str) -> str:
    """Print header line."""
    header = text
    print("-" * len(header))
    print(header)
    print("-" * len(header))
